import { MantineTheme } from '@mantine/core';

export const theme: Partial<MantineTheme> = {
  defaultRadius: 'sm',
  cursorType: 'pointer',
  defaultGradient: {
    from: 'cyan',
    to: 'blue',
    deg: 45,
  },
};
